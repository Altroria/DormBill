# 蓉蓉的收租小工具 - 项目开发规范

## 项目概述
这是一个宿舍房租水电费用管理系统，基于 FastAPI + Vue3 + MySQL 技术栈。

## 技术栈
- 后端: Python 3.10+ / FastAPI / SQLAlchemy 2.0 / Pydantic v2
- 前端: Vue 3 + TypeScript + Vite / Element Plus
- 数据库: MySQL 8.0
- 部署: Docker / Docker Compose

## 代码规范

### Python 后端规范
- 使用 Python 3.10+ 特性（类型注解、dataclass 等）
- 遵循 PEP 8 规范，使用 4 个空格缩进
- 使用 SQLAlchemy 2.0 的异步 ORM 风格
- Pydantic v2 模型用于请求/响应验证
- 所有 API 端点必须有完整的类型注解和文档字符串
- 数据库操作使用异步会话（AsyncSession）
- 错误处理使用 HTTPException，返回清晰的错误信息

### TypeScript 前端规范
- 使用 Vue 3 Composition API（<script setup lang="ts">）
- 所有变量和函数必须有明确的 TypeScript 类型
- 使用 Element Plus 组件库，保持 UI 一致性
- API 调用使用 axios，统一错误处理
- 使用 ref/reactive 管理响应式状态
- 组件命名使用 PascalCase

### 数据库规范
- 表名使用复数形式（如 buildings, rooms, employees）
- 主键统一使用 id（整数自增）
- 时间字段使用 created_at, updated_at
- 软删除使用 deleted_at
- 外键关系明确，使用 relationship 定义
- 所有表必须有注释（comment）

## 项目结构
```
DormBill/
├── backend/              # FastAPI 后端
│   ├── app/
│   │   ├── api/         # API 路由
│   │   ├── core/        # 核心配置
│   │   ├── models/      # SQLAlchemy 模型
│   │   ├── schemas/     # Pydantic 模式
│   │   └── services/    # 业务逻辑
│   └── requirements.txt
├── frontend/            # Vue3 前端
│   ├── src/
│   │   ├── api/        # API 调用
│   │   ├── components/ # 组件
│   │   ├── views/      # 页面
│   │   └── types/      # TypeScript 类型
│   └── package.json
└── docker-compose.yml  # Docker 配置
```

## 开发约定

### API 设计
- RESTful 风格，使用标准 HTTP 方法
- 路径格式: `/api/v1/{resource}`
- 分页参数: `skip` (偏移) 和 `limit` (限制)
- 响应格式统一: `{ data, message, code }`

### 命名约定
- Python 变量/函数: snake_case
- Python 类: PascalCase
- TypeScript 变量/函数: camelCase
- TypeScript 类/组件: PascalCase
- 常量: UPPER_SNAKE_CASE

### 业务规则
- 月度电费结算（每月）：电费 = (本次读数 - 上次读数) × 单价，按入住天数分摊
- 双月水费结算（两月）：水费按半月规则分摊到两个月
- 试用期免租金，不免水电
- 夫妻间费用由丈夫承担
- 出差豁免：当月出差天数 >= 15天，免水电
- 特殊房间：211A/212A 只收电费不收房租

### Git 规范
- 提交信息格式: `<type>: <description>`
- type: feat / fix / docs / style / refactor / test / chore
- 中文或英文均可，保持清晰明确

## 常用命令

### 开发环境
```bash
# 激活虚拟环境（Windows）
.\venv\Scripts\Activate.ps1

# 后端开发服务器
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# 前端开发服务器
cd frontend
npm run dev
```

### Docker 部署
```bash
# 开发环境
docker-compose up -d

# 生产环境
docker-compose -f docker-compose.prod.yml up -d
```

## 注意事项
- 敏感信息（密码、密钥）必须使用环境变量，不要提交到代码仓库
- 数据库迁移使用 Alembic，不要直接修改数据库结构
- 前端状态管理优先使用 Composition API，复杂场景考虑 Pinia
- 所有用户输入必须验证和转义，防止 SQL 注入和 XSS
- Excel 导入导出需要处理大文件，使用流式处理
