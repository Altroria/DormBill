# 电表首次录入功能更新

## 🎯 更新目标

解决系统首次使用时,没有历史数据导致"上月读数"默认为0的问题。

## ✅ 已完成

### 后端修改

1. **`backend/app/schemas/meter.py`**
   - `MeterUpdate` 增加 `previous_reading` 和 `ac_previous_reading` 字段

2. **`backend/app/services/meter_service.py`**
   - `update_meter_readings` 函数增加上月读数参数
   - 支持手动更新上月读数

3. **`backend/app/routers/meters.py`**
   - `update_meter` 端点传递上月读数参数

### 前端修改

4. **`frontend/src/views/MeterManage.vue`**
   - 增加 `isFirstTime` 检测(上月读数=0时触发)
   - 上月读数字段支持编辑(仅首次录入时)
   - 显示黄色提示框提醒首次录入
   - 表单验证逻辑适配首次录入场景

## 🔍 功能特性

### 首次录入模式

当检测到 `previous_reading === 0 && ac_previous_reading === 0` 时:

✅ 显示黄色提示框  
✅ 上月读数字段变为可编辑  
✅ 用户可手动填写实际的上月读数  
✅ 验证逻辑: 本月读数 ≥ 手动填写的上月读数

### 常规录入模式

已有历史数据时:

✅ 上月读数字段灰色锁定(不可编辑)  
✅ 自动带出上月的"本月读数"  
✅ 验证逻辑: 本月读数 ≥ 系统带出的上月读数

## 📋 使用场景

### 场景1: 系统首次上线(7月)

```
初始化7月电表 → 上月读数=0
录入时:
  ✏️ 上月普通读数: 5000.00 (手动填写6月底实际读数)
  ✏️ 本月普通读数: 5150.00
  ✏️ 上月空调读数: 1200.00 (手动填写6月底实际读数)
  ✏️ 本月空调读数: 1250.00
```

### 场景2: 第二个月开始(8月)

```
初始化8月电表 → 自动带出7月读数
录入时:
  🔒 上月普通读数: 5150.00 (灰色锁定,自动带出)
  ✏️ 本月普通读数: 5300.00
  🔒 上月空调读数: 1250.00 (灰色锁定,自动带出)
  ✏️ 本月空调读数: 1300.00
```

## 🎨 UI 变化

### 录入对话框新增提示

```html
⚠️ 首次录入提示
系统检测到上月读数为0,这可能是首次录入。
请手动填写上月读数作为起始值。
```

### 字段状态

| 字段 | 首次录入 | 后续月份 |
|------|---------|---------|
| 上月普通读数 | ✏️ 可编辑 | 🔒 锁定 |
| 本月普通读数 | ✏️ 可编辑 | ✏️ 可编辑 |
| 上月空调读数 | ✏️ 可编辑 | 🔒 锁定 |
| 本月空调读数 | ✏️ 可编辑 | ✏️ 可编辑 |

## 🔄 兼容性

✅ **向后兼容**: 不影响已有数据  
✅ **自动检测**: 无需手动配置  
✅ **平滑过渡**: 首次录入后自动切换为常规模式  
✅ **无需迁移**: 不涉及数据库结构变更

## 🚀 部署说明

后端已自动热重载(uvicorn --reload),无需手动重启。  
前端需要重新编译或刷新浏览器。

---

**更新时间**: 2026-09-09  
**涉及文件**: 4个文件修改
