# 首次录入电表读数说明

## 问题说明

系统首次使用时,没有历史数据,初始化电表记录时"上月读数"会默认为 `0`,这不符合实际情况。

## 解决方案

**已支持首次录入时手动填写"上月读数"作为起始值。**

---

## 操作流程

### 1️⃣ 初始化月度电表

在**电表管理**页面:
```
1. 选择月份 (例如: 2026-09)
2. 可选择楼栋筛选
3. 点击 "初始化月度电表" 按钮
```

系统会创建本月的电表记录,上月读数默认为 `0`。

### 2️⃣ 录入电表读数

点击表格中某个房间的 **"录入"** 按钮,弹出录入对话框:

#### 🆕 首次录入(上月读数为0时)

系统会自动检测并显示**黄色提示框**:

> ⚠️ **首次录入提示**  
> 系统检测到上月读数为0,这可能是首次录入。请手动填写上月读数作为起始值。

此时对话框中:
- ✏️ **上月普通读数**: **可编辑** - 手动填写实际的上月读数
- ✏️ **本月普通读数**: **必填** - 填写本月普通电表读数
- ✏️ **上月空调读数**: **可编辑** - 手动填写实际的上月空调读数
- ✏️ **本月空调读数**: **必填** - 填写本月空调电表读数
  - 如果房间**没有空调电表**,填写 `0`

#### 📋 后续月份录入(已有历史数据)

上月读数字段变为**灰色不可编辑**,系统自动带出上月的"本月读数"。

---

## 录入示例

### 示例1: 有空调电表的房间(首次录入)

```
房间: 247-201-小小间
上月普通读数: 5000.00  ← 手动填写实际上月读数
本月普通读数: 5150.00  ← 本月抄表值
上月空调读数: 1200.00  ← 手动填写实际上月读数
本月空调读数: 1250.00  ← 本月抄表值

结果:
- 普通用电量 = 150度
- 空调用电量 = 50度
```

### 示例2: 没有空调电表的房间(首次录入)

```
房间: 247-202-大大间
上月普通读数: 3000.00  ← 手动填写实际上月读数
本月普通读数: 3180.00  ← 本月抄表值
上月空调读数: 0         ← 没有空调表,填0
本月空调读数: 0         ← 没有空调表,填0

结果:
- 普通用电量 = 180度 (包含空调用电)
- 空调用电量 = 0度
- 空调费 = 0元 (已在普通电费中计算)
```

---

## 注意事项

1. ✅ **首次录入时**,上月读数字段**可编辑**,请务必填写实际值
2. ✅ **后续月份**,上月读数自动带出,**不可编辑**
3. ✅ **没有空调电表**的房间,空调读数填 `0`,系统会自动识别 `空调费=0`
4. ✅ 本月读数必须 **≥ 上月读数**,否则会提示错误
5. ✅ 录入完成后,点击"批量计算电费"统一计算

---

## 技术实现

### 后端修改

- `MeterUpdate` Schema: 增加 `previous_reading` 和 `ac_previous_reading` 字段
- `update_meter_readings`: 支持更新上月读数
- `meters.py` Router: 传递上月读数参数

### 前端修改

- 增加 `isFirstTime` 检测逻辑(上月读数=0时)
- 上月读数字段支持编辑(首次录入时)
- 显示黄色提示框提醒用户
- 后续月份自动锁定上月读数字段

---

## 业务规则确认

### 空调电费规则

1. **有独立空调电表的房间:**
   - 空调用电单独计量
   - 空调费 = 空调度数 × 空调单价
   - 房间内人员平均分摊

2. **没有独立空调电表的房间:**
   - ✅ 有空调设备
   - ✅ 空调用电已包含在普通电表里
   - ✅ 空调费 = 0 (不重复计算)
   - ✅ 普通电费正常计算(已包含空调用电)

---

## 更新日期

2026-09-09
